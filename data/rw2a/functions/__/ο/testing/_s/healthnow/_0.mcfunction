

# last_health last_absorb last_total health absorb total delta_health delta_absorb delta_total

#$say HPFX-Last health:$(last_health)     absorb:$(last_absorb)     total:$(last_total)
#$say HPFX-Now  health:$(health)($(delta_health)) absorb:$(absorb)($(delta_absorb)) total:$(total)($(delta_total))

$say HPFX health:$(health) absorb:$(absorb) total:$(total)